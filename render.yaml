databases:
  - name: cursodjango
    plan: free
    databaseName: cursodjango
    user: cursodjango

services:
  - type: web
    name: cursodjango
    plan: free
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn mysite.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.5
      - key: DATABASE_URL
        fromDatabase:
          name: cursodjango
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
